import React from "react";
import NavTabs from "./Navigation";

const styles = {
  header: {
    background: "white",
    fontSize: "40px",
    color: "black",
  },
};

function Header() {
  return <NavTabs />;
}

export default Header;
